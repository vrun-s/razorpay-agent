# Handoff — Razorpay Revenue Recovery (Buildathon Track 3)

**Repo:** `C:\Projects\razorpay\razorpay-agent` (git branch `master`, working tree clean at time of writing)
**Next session's focus (per user):** testing halted subscriptions (finishing ticket 17's last checkbox) and implementing ticket 18.

## Project in one line

See `CONTEXT.md` (repo root) for the domain glossary — don't re-derive it here.

## State: tickets 01–17 done (17 resolved with one documented gap)

```
abc2e7e feat: EventSource.REAL tagging for real-Razorpay cases (ticket 17)
44e2734 feat: misspecification stress test (ticket 16)
30e8a80 feat: evaluation harness -- NRR, baselines, bootstrap CI (ticket 15)
d8f8ba1 feat: wire simulator generator into the Gateway seam (ticket 14)
54abc03 docs: handoff for ticket 13
```

Repo convention unchanged: every ticket lands as a direct commit to local `master`, no per-ticket branches/PRs; `origin/master` stays far behind and untouched (intentional, solo build). Issue-file checkboxes under `.scratch/recovery-engine/issues/` are **not** edited after completion (confirmed against ticket 13's file, still shows unchecked boxes) — completion state lives in handoffs/commits/memory instead.

## Canonical sources of truth (read these, don't ask the user to re-derive them)

- `CONTEXT.md` (repo root) — domain glossary.
- `docs/adr/0001`–`0013` — every architectural decision.
- `.scratch/recovery-engine/spec.md` — the build spec (53 user stories).
- `.scratch/recovery-engine/issues/18-dashboard-demo-observability.md` — this session's ticket, with its own `- [ ]` acceptance criteria. **Read in full before starting.**
- `docs/agent-handoffs/2026-08-25-handoff.md` — prior handoff covering tickets 01–13 in detail and the full 13–18 dependency graph. Still accurate.
- User-level memory (auto-loaded, see `MEMORY.md`) — `project_ticket17_pending_manual_execution.md` has the full ticket 17 story: three iterations of a real-API test script, two subtle bugs found and fixed (a "processed but not executed" phantom-row bug, and a retry-wrapper that re-ran an internal DB commit), and the final resolution. Don't re-derive that debugging narrative; it's captured there.

## Ticket 17 — resolved this session (2026-08-27)

- **Failed-payment half: done and verified.** 20/20 hand-picked cases ran through the real `RazorpayGateway`, each confirmed (via direct sqlite query against `backend/recovery.db`) to have exactly one `EXECUTION` `CaseHistoryEntry` and `source=EventSource.REAL`. User confirmed all 20 Payment Links visible in the Razorpay test-mode dashboard, `status=created`. Stays within the 30-link cap (21/30 used, including one leftover from ticket 13).
- **Halted-subscription half: deliberately left open.** The user has a real test-mode subscription, `sub_TSiXXXH44OuIe5`, but it has only ever shown `active` — never transitioned to `halted`/`paused` (this requires Razorpay's own retry schedule to elapse, outside the user's control). Attempting `resume_charge` against an `active` subscription would likely be rejected by Razorpay's API (documented for `paused` only), and separately the Decision Engine isn't guaranteed to even pick `RESUME_CHARGE` over `NO_ACTION` for it (`backend/app/decision.py`'s `VALID_INTERVENTIONS` is probabilistic once the estimator's posterior has moved past cold start). User explicitly chose to close ticket 17 with this gap documented rather than force a low-value attempt.
- The manual-execution script lives at `test-scripts/run_ticket17_real_slice.py` (gitignored, untracked — matches ticket 13's precedent). It already has a `HALTED_SUBSCRIPTION_CASES` list with a commented-out example slot ready to fill in once a subscription genuinely reaches `halted`/`paused`. Run from `backend/`: `uv run python ../test-scripts/run_ticket17_real_slice.py`.

### If this session is "testing halted subscriptions"

1. Ask the user whether `sub_TSiXXXH44OuIe5` (or a new one) has reached `halted`/`paused` yet. **Don't attempt `resume_charge` against an `active` subscription** — see reasoning above.
2. If it has: uncomment/fill `HALTED_SUBSCRIPTION_CASES` in the script with `{"id": "sub_...", "plan_id": "plan_..."}`, run the script, then verify the same way ticket 17's failed-payment half was verified — an `EXECUTION` `CaseHistoryEntry` for that case's id, `source=REAL` — **not** just that the script printed a success line (see the memory doc's gotcha #2: the script can create committed-but-not-executed "phantom" rows).
3. If the Decision Engine picks `NO_ACTION` instead of `RESUME_CHARGE` for it, the case won't produce an `EXECUTION` entry either — that's a legitimate outcome per ticket 07's design, not a bug, but it means the ticket 17 checkbox ("exercises Resume Charge") still isn't satisfied by that run.

## Ticket 18 — not started, unblocked

Blocked by 09, 10, 15 — all three are done, so ticket 18 is ready to start. Full acceptance criteria in `.scratch/recovery-engine/issues/18-dashboard-demo-observability.md` — read it fresh; summary only below:

- A live case-timeline view (detected → decision+reasoning → policy check → execution → webhook → reassessment → stop, timestamped).
- Reserved Budget shown as a moving quantity over a run.
- At least one `NO_ACTION`-that-still-recovered case, one policy rejection, and one human-overridden escalation each browsable in the dashboard.
- An aggregate view: Net Recovered Revenue vs. both baselines with a confidence interval and % of offline-optimal, sourced from ticket 15's evaluation harness output.
- The calibration curve from ticket 15, displayed.

**Existing scaffold** — `frontend/` (Vite + React + TS), built incrementally across tickets 01/06/09/10. Current `frontend/src/` contents: `App.tsx`, `EscalationQueue.tsx`, `api.ts`, `main.tsx`, `index.css`. Ticket 18 is additive on top of this, not a fresh build — check what `api.ts`/`App.tsx` already expose before adding new views.

**Backend data source for the aggregate view** — `backend/app/evaluation.py`: `run_evaluation()` (line ~543) returns an `EvaluationReport` (line ~523) with `calibration: list[CalibrationBucket]` (from `calibration_curve()`, line ~491) and the NRR/baseline/bootstrap-CI numbers ticket 15 built. No existing HTTP route was found exposing this to the frontend in a quick check this session — confirm whether one exists before assuming ticket 18 needs to add it.

## Known gaps still open (carried over, not re-verified this session)

- `ProposedIntervention.incentive_amount`/`discount_pct` still always `0` (since ticket 05/ADR-0010).
- `PolicyConfig.recovery_budget` / `BudgetLedger.recovery_budget` still two independently-configured views of the same ceiling, not reconciled.
- No outcome-relevant webhook route for halted-subscription cases (a `subscription.charged` analog to `/payment-captured`).
- Halted-subscription `case_value`/`avg_order_value` always resolve to `0` — `escalation_value_threshold` can never fire for this workflow.

## Suggested skills for the next session

- **`implement`** — `/implement ticket 18`. Same TDD-at-seams + full-suite + `/code-review`-before-commit flow every prior ticket has used. For the halted-subscription testing side-task, `implement` is overkill — that's a manual verification step, not new feature code (unless the subscription has actually halted and needs the script edit in step 2 above, which is a small enough change to do directly).
- **`code-review`** — run before any ticket 18 commit, same as always.
- **`frontend-design`** — ticket 18 is UI-heavy and judge-facing ("making the system's strongest ideas visible"); worth loading for guidance on making the dashboard's visual choices intentional rather than default-template, before writing the new views.
